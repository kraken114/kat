# рабочая для входов
def in_for_mAP_true(batch):
  lst_boxes=batch[1]

  for i,_ in enumerate(lst):
    t=list(zip(_[0],_[1]))
    # print(t)
    lst_data =[]
    for k in t:

w
  return ground_date

for i, _ in enumerate(lst):
  t = list(zip(_[0], _[1]))
  # print(t)
  lst_data = []
  for k in t:
    for i, _ in enumerate(lst):
      t = list(zip(_[0], _[1]))
      # print(t)
      lst_data = []
      for k in  for i,_ in enumerate(lst):
    t=list(zip(_[0],_[1]))
    # print(t)
    lst_data =[]
    for k in t:  for i,_ in enumerate(lst):
    t=list(zip(_[0],_[1]))
    # print(t)
    lst_data =[]
    for k in t: t: